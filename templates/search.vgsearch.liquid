{% layout none %}{% capture results %}{% paginate search.results by 25 %}{% for item in search.results %}{% assign product = item %}{"id":{{product.id | json}},"images":{{product.images | json}},"title":{{ product.title | json }},"vendor":{{ product.vendor | json }},"variants":{{product.variants|json}}}{% unless forloop.last %},{% endunless %}{% endfor %}{% endpaginate %}{% endcapture %}{"products":[{{results}}]}